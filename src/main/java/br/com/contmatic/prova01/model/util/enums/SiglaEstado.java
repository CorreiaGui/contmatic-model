package br.com.contmatic.prova01.model.util.enums;

/**
 * The Enum SiglaEstado.
 *
 * @author guilherme.martins
 */
public enum SiglaEstado {

                         /** The sp. */
                         SP,
                         /** The rj. */
                         RJ,
                         /** The ma. */
                         MA,
                         /** The go. */
                         GO,
                         /** The ac. */
                         AC,
                         /** The al. */
                         AL,
                         /** The ap. */
                         AP,
                         /** The ba. */
                         BA,
                         /** The ce. */
                         CE,
                         /** The es. */
                         ES,
                         /** The rs. */
                         RS,
                         /** The rn. */
                         RN,
                         /** The pi. */
                         PI,
                         /** The pe. */
                         PE,
                         /** The pr. */
                         PR,
                         /** The pb. */
                         PB,
                         /** The pa. */
                         PA,
                         /** The mg. */
                         MG,
                         /** The ms. */
                         MS,
                         /** The mt. */
                         MT,
                         /** The ro. */
                         RO,
                         /** The rr. */
                         RR,
                         /** The sc. */
                         SC,
                         /** The se. */
                         SE,
                         /** The to. */
                         TO,
                         /** The df. */
                         DF
}
